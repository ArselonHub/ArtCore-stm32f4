/*
 * PulseChannel.h
 *
 *  Created on: May 10, 2017
 *      Author: denizcan
 */

#ifndef ART_PULSECHANNEL_H_
#define ART_PULSECHANNEL_H_

#include <Art/Pit.h>
#include <Art/Device.h>

namespace Art
{
/*

	class PulseChannel : public Device
	{
	public:
		virtual ~PulseChannel();
		void setValue(UWord value);
		PulseChannel* port() const;
		void setPort(PulseChannel* port);

	private:
		virtual Word openDevice();
		virtual Word closeDevice();
		virtual Word enableDevice();
		virtual Word disableDevice();

		PulseChannel* m_port;
		UWord m_value;
	};

	inline PulseChannel* PulseChannel::port() const
	{
		return m_port;
	}
*/
}

#endif /* SOURCE_ART_PULSECHANNEL_H_ */
